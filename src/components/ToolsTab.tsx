interface ToolsTabProps {
    bionic: boolean;
    setBionic: (value: boolean) => void;
}

const ToolsTab: React.FC<ToolsTabProps> = ({ bionic, setBionic }) => {
    return (
        <></>
    );
};

export default ToolsTab; 